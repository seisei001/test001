{
  "$schema": "./schemas/system.schema.json",
  "model": {
    "coreModelUrl": "PLACEHOLDER_未確定。DESIGN.md 5.1節・8.2節参照(Qwen2.5-0.5B-Instruct級を第一候補としてPhase 0で実機検証要)",
    "quantization": "int4",
    "rawEmbeddingDim": 768,
    "maxSequenceLength": 128,
    "expectedSizeMB": "未確定(Phase 0で検証)",
    "mockMode": true,
    "note": "embedding抽出と文章生成の両方をこの1モデルで兼ねる。base weightsは常にfreeze、更新対象はLoRAアダプタのみ(DESIGN.md 1.3節)。mockModeがtrueの間、CoreModelはネットワーク不要な決定論的スタブで動作する(huggingface.co/cdn.jsdelivr.netへのアクセスが無い開発環境でもパイプライン全体を検証できるようにするため)。coreModelUrlが確定しモデルの実機検証が済んだらfalseにする。"
  },
  "runtime": {
    "backend": "webgpu",
    "fallbackBackend": "wasm",
    "numThreads": 4,
    "warmupOnLoad": true
  },
  "storage": {
    "indexedDBName": "smart-box-db",
    "indexedDBVersion": 1,
    "maxTurnsStored": 1000,
    "compressAfterTurns": 100,
    "quotaWarningMB": 40,
    "quotaHardLimitMB": 80
  },
  "logging": {
    "enabled": true,
    "level": "info",
    "persistToIndexedDB": true,
    "exportFormats": ["json", "csv"]
  },
  "experiments": {
    "abTestingEnabled": true,
    "stickyAssignment": true
  },
  "targets": {
    "note": "生成モデルへの変更に伴い目標値は未確定(Phase 0で実機検証してから設定する)。",
    "maxInferenceLatencyMs": "TBD",
    "maxTotalTurnLatencyMs": "TBD"
  }
}
